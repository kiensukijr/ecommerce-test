{
  "name": "ecommerce-automation-test-root",
  "version": "1.0.0",
  "description": "Root workspace for e-commerce website designed for automation testing",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:ui": "playwright test tests-playwright/specs/ui",
    "test:api": "playwright test tests-playwright/specs/api",
    "test:db": "playwright test tests-playwright/specs/database"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.1",
    "@types/node": "^25.9.2",
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "mongodb": "^6.5.0",
    "wait-on": "^9.0.10"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "mongoose": "^9.6.3"
  }
}
